var added_comment = $("<%= j(render @comment) %>");

added_comment.hide();

$("#<%= dom_id(@comment.photo) %>_photo_comments").after(added_comment);

added_comment.slideDown();

$("#<%= dom_id(@comment.photo) %>_photo_comments #comment_body").val("");
